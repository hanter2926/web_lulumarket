// Minimal JS for checkout address interactions
// Placeholder to prevent missing static manifest errors
document.addEventListener('DOMContentLoaded', function () {
    // Simple enhancement: focus first input if present
    var firstInput = document.querySelector('#addressForm input, #addressForm textarea');
    if (firstInput) firstInput.focus();
});
