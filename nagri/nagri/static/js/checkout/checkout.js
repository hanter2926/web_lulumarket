// Minimal checkout helpers (kept intentionally small)
// The project already defines getCookie in base/main.js; this file can be used
// for any future checkout related helpers.

// Placeholder to avoid 404 when referenced in templates
(function () {
    // noop
})();
