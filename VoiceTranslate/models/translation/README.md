# Translation Models

## Planned use

SeamlessM4T will provide translation behind an isolated service interface.

## Requirements

Model revision, language coverage, device, and cache directory must be configurable. CPU development may require a smaller or offline test double; production GPU workers should reserve VRAM for model weights and inference batches.

Do not download or load SeamlessM4T yet.
